using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;



namespace BLL
{
    class dialogresult
    {
        public void test()
        {
            //DialogResult dialogResult = MessageBox.Show("La pièce est déja ajoutée à la commande client!! 'Oui' pour additioner la quantité 'Non' pour annuler   ", "Attention", MessageBoxButtons.YesNo, MessageBoxIcon.Information);

            //if (dialogResult == DialogResult.Yes)
            //{

             
            //}
            //else if (dialogResult == DialogResult.No)
            //{

            //}
        }
        public void update()
        {
            //var sql = @"Update Article SET quantitenstock = @quantitenstock ,quantitedisponible=@quantitedisponible WHERE codeart = @codeart";

            //db.Database.ExecuteSqlCommand(
            //    sql,
            //    new SqlParameter("@quantitenstock", art.quantitenstock),
            //    new SqlParameter("@quantitedisponible", art.quantitenstock),
            //    new SqlParameter("@codeart", art.codeart));
        }
    }
}
